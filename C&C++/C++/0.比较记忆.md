- `int *p[SIZE];`指针数组：是数组，数组内元素是指针
- `int *p();`指针函数：是函数，函数返回值是指针  
- `int *const p = &n;`指针常量：是常量，常量的内容是指针  
---
- `int (*p)[SIZE];`数组指针：是指针，指针指向数组首地址
- `int (*p)();`函数指针：是指针，指针指向函数入口
- `int const *p = const int *p`常量指针：是指针，指针指向常量